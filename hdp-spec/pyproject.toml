[project]
name = "hdp-proto"
version = "0.0.0"
description = "Stdlib-only HDP protocol codec"
requires-python = ">=3.11,<3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["hdp_proto"]

